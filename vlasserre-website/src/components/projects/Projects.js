import React from "react";
import ProjectBox from "../projectbox/ProjectBox";

const Projects = ({ details }) => {
    return (
        <ProjectBox details={details} />
    )
}

export default Projects;